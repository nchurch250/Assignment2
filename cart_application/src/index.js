import React from 'react';
import ReactDOM from 'react-dom/client';
import ProductPage from './script';

const root = ReactDOM.createRoot(document.getElementById('root'));


root.render(
  <React.StrictMode>
    <ProductPage />
  </React.StrictMode>
);

